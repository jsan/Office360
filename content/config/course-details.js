

$(document).ready(function ( ) {
	var CourseTitle = 'Cortex Virtual Office';
	
	//The sourse of the Submit Button to be used for the course.
	var subButt = "img/submit.png";
	var hasFullScr = true;
	
	
	document.title = CourseTitle;
	$('.course-title').html(CourseTitle);
});
